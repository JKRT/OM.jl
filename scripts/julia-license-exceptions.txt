# License-check exceptions for scripts/check_license.jl
#
# One entry per line. Patterns are evaluated in order; last match wins
# (.gitignore semantics). Blank lines and lines starting with `#` are ignored.
#
#   path/relative/to/root      exact path or directory (excluded)
#   glob/pattern/**/*.jl       glob pattern, relative to root
#   !path/relative/to/root     negation; re-includes a previously excluded path
#
# Skip vendored / generated trees by default.
**/Manifest.toml
**/.git/**
**/build/**
**/.julia/**
